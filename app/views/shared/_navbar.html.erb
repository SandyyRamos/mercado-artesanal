 <nav class="navbar navbar-expand-lg navbar-light bg-light shadow-sm sticky-top">
      <div class="container">
        <a class="navbar-brand" href="<%= root_path %>">
  <%= image_tag("mercado_artesanal_color.png", alt: "Mercado Artesanal Logo", class: "img-fluid img-hover-opacity", style: "max-width: 200px;") %>
</a>

        <!-- Barra de búsqueda -->
        <form class="d-flex ms-auto" style="max-width: 500px; flex-grow: 1;">
          <input class="form-control me-2" type="search" placeholder="Search products..." aria-label="Search" style="max-width: 100%;"/>
          <button class="btn btn-outline-custom" type="submit">
            <i class="bi bi-search" style="color: #795f76;"></i>
          </button>
        </form>


<!-- Formulario de búsqueda -->
<div class="row justify-content-center">
  <div class="col-sm-8 my-3">
    <%= form_with url: root_path, method: :get, class: "d-flex" do |f| %>
      <%= text_field_tag :query,
            params[:query],
            class: "form-control" ,
            placeholder: "Buscar productos..."
        %>
          <%= f.button "<i class='bi bi-search' style='color: #c65773;'></i>".html_safe, class: "btn btn-outline-custom" %>
    <% end %>
  </div>
</div>
<!-- Fin del formulario -->

        <!-- Menú de navegación -->
        <div class="collapse navbar-collapse">
  <ul class="navbar-nav ms-auto">
    <li class="nav-item <%= 'active' if current_page?(root_path) %>">
      <%= link_to 'Home', root_path, class: 'nav-link custom-link-color' %>
    </li>
    <li class="nav-item <%= 'active' if current_page?(products_path) %>">
      <%= link_to 'Products', products_path, class: 'nav-link custom-link-color' %>
    </li>
    <li class="nav-item <%= 'active' if current_page?(orders_path) %>">
      <%= link_to 'My Orders', orders_path, class: 'nav-link custom-link-color' %>
    </li>
    <li class="nav-item <%= 'active' if current_page?(my_sales_path) %>">
      <%= link_to 'My Sales', my_sales_path, class: 'nav-link custom-link-color' %>
    </li>
    <li class="nav-item <%= 'active' if current_page?('/users/sign_in') %>">
  <%= link_to 'Sign In', '/users/sign_in', class: 'nav-link custom-link-color' %>
</li>
  </ul>
</div>

      </div>
    </nav>
